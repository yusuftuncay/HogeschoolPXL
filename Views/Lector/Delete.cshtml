@model HogeschoolPXL.Models.Lector

@{
    ViewData["Title"] = "Verwijderen";
}

<h3 class="titleView">Deze lector verwijderen?</h3>

<div class="d-flex justify-content-center">
    <div class="col-md-4">
        <dl class="row">
            <dt class="col-sm-4">
                @Html.DisplayNameFor(model => model.Gebruiker)
            </dt>
            <dd class="col-sm-8">
                @Html.DisplayFor(model => model.Gebruiker.Voornaam) 
                @Html.DisplayFor(model => model.Gebruiker.Naam)
            </dd>
        </dl>
    
        <form asp-action="Delete" class="form-group d-flex justify-content-between">
            <a class="btn-sm backArrow" asp-action="Index" asp-controller="Lector">
                <img src="~/img/arrow-left-black.png" height="16"/>
                <span >Terug</span>
            </a>
            <input type="hidden" asp-for="LectorId"/>
            <input type="submit" value="Verwijder" class="btn btn-outline-danger btn-sm viewButton"/>
        </form>
    </div>
</div>
