@model IEnumerable<HogeschoolPXL.Models.Lector>

@{
    ViewData["Title"] = "Overview";
}

<h1>Overview</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Gebruiker)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Gebruiker.VoorNaam)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LectorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LectorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LectorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
